{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infrastructure/Dockerfile.api"
  },
  "deploy": {
    "startCommand": "/bin/sh -c \"exec uvicorn apps.api.main:app --host 0.0.0.0 --port $PORT\""
  }
}
